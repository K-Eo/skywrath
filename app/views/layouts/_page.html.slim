- if defined?(@flash) && @flash
  = render "layouts/flash"
.container.mt-4#content-body
  .row
    - if defined?(@sidebar) && @sidebar
      .col
        = render "layouts/sidebar/#{@sidebar}"
    = yield
